package com.vicgarci.kgbem.cpu

object FlagsRegisterFixtures {

    val FLAGS_NOT_SET = FlagsRegister(
        zero = false,
        subtract = false,
        halfCarry = false,
        carry = false,
    )
}
